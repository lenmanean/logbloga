# Supabase Email Template for OTP Verification

## Instructions
1. Go to **Supabase Dashboard → Authentication → Email Templates**
2. Find the **"Confirm signup"** template (or "Magic Link" template)
3. Replace the entire template content with the HTML below
4. Click **"Save"**

## HTML Template

```html
<h2>Confirm your signup</h2>

<p>Hi there,</p>

<p>Thank you for signing up! Please enter the verification code below to confirm your email address:</p>

<p style="font-size: 32px; font-weight: bold; letter-spacing: 8px; text-align: center; color: #000; margin: 24px 0;">
  {{ .Token }}
</p>

<p>This code will expire in 1 hour.</p>

<p>If you didn't request this code, you can safely ignore this email.</p>

<p>Best regards,<br>The Logbloga Team</p>
```

## Alternative Simple Template

If you prefer a simpler version:

```html
<h2>Confirm your signup</h2>

<p>Your verification code is: <strong>{{ .Token }}</strong></p>

<p>Enter this 6-digit code to complete your signup.</p>

<p>This code expires in 1 hour.</p>
```

## Important Notes

- `{{ .Token }}` is the variable that contains the OTP code (typically 6-8 digits)
- Do NOT include `{{ .ConfirmationURL }}` or any link variables
- The code is automatically generated by Supabase
- Users will enter this code on the verification page
- The code length may vary (6 or 8 digits) depending on Supabase configuration
